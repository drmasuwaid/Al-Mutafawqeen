rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Homework files are served only through the Next.js Admin SDK
    // at /api/attachments. Direct client access is denied.
    match /homework/{homeworkId}/{fileName} {
      allow read, write: if false;
    }
  }
}
